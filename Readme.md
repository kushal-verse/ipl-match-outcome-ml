# IPL Match Outcome Prediction (ML)

Machine learning project to predict IPL match outcomes using historical match data, team statistics, and contextual features.

## Goals
- Build reproducible ML pipeline
- Experiment with multiple models
- Provide win probability predictions

## Project Structure
Explain folders briefly.

## Setup
```bash
pip install -r requirements.txt